name: New Pull Request

on:
  pull_request:
    types:
      - opened
      - reopened

jobs:
  add-to-project:
    name: Add issue to BeeWare PR project board
    runs-on: ubuntu-latest
    # The add-to-project action authenticates with BRUTUS_PAT_TOKEN, so the
    # job's own GITHUB_TOKEN needs no permissions.
    permissions: {}
    steps:
      - uses: actions/add-to-project@5afcf98fcd03f1c2f92c3c83f58ae24323cc57fd  # v2.0.0
        with:
          project-url: https://github.com/orgs/beeware/projects/3 # Pull Request board
          github-token: ${{ secrets.BRUTUS_PAT_TOKEN }}
